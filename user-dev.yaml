spring:
  datasource:
    url: jdbc:mysql://host.docker.internal:3306/chatapp_user

eureka:
  instance:
    hostname: host.docker.internal
  client:
    serviceUrl:
      defaultZone: http://host.docker.internal:8761/eureka/
